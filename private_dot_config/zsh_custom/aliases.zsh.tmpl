alias ls='lsd'

alias l='ls -l'
alias la='ls -a'
alias lla='ls -la'
alias lt='ls --tree'

alias rmr='rm -r'

alias cat='bat --paging=never --style=plain'

[[ "$TERM" == "xterm-kitty" ]] && alias ssh="kitty +kitten ssh"
