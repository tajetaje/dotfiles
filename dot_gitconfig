# Main settings
[user]
	name = Tag Howard
	email = tag.howard1080@gmail.com
[core]
    pager = delta
    autocrlf = input

[init]
	defaultBranch = main

# Standard UI settings
[color]
    ui = true

[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green

[color "diff"]
    meta = yellow bold
    frag = magenta bold
    old = red bold
    new = green bold

[color "status"]
    added = yellow
    changed = green
    untracked = red

[merge]
    conflictstyle = diff3

[diff]
    colorMoved = default

# Delta settings
[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true  # use n and N to move between diff sections
    line-numbers = true
